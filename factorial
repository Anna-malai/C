/*
Factorial for given numbers
Input: n=7
output: Factorial for given number is 5040
*/
#include<stdio.h>
#include<conio.h>
int main()
 {
  int n,i;
  long f;
  clrscr();
  printf("Enter n");
  scanf("%d",&n);
  f=1;
  for(i=1;i<=n;i++)
  f=f*i;
  printf("factorial for given number is %d",f)
 }
