#include <stdio.h>
int main() 
{
int j,n,array[50];
scanf("%d",&n)
for(j=0;j<n;++j)
scanf("%d",&array[j]); /*storing the elements of array from test case */
int largest;
largest=array[0];
for(j=1;j<5;j++)
{
 if(array[j]>largest)
   largest=array[j];
}
 printf("%d", largest); 
}
